<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.popis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Popis">
        <activity android:name=".creditscreen"></activity>
        <activity android:name=".meal_reciept" />
        <activity android:name=".update_remove" />
        <activity android:name=".add_new" />
        <activity android:name=".data_workers" />
        <activity android:name=".data_companies" />
        <activity android:name=".data_orders" />
        <activity android:name=".add_new_meal" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>